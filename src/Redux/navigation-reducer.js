let initialState = {
    friends: [
        {
            name: 'Mia'
        },
        {
            name: 'Nick'
        },
        {
            name: 'Depression'
        }
    ]
}

const navigationReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}

export default navigationReducer;